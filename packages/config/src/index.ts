export const appName = "Learnable";

export const navItems = [
  { href: "/", label: "Home" }
] as const;

export const researchDefaults = {
  maxRecursionDepth: 3,
  maxSpawnedNodes: 12,
  maxEvidencePerNode: 8
} as const;
